Corrected Sourdough Field Guide HTML/CSS cleanup

What this contains:
- Top-level cleaned HTML files
- /recipes/ cleaned recipe HTML files
- /recipes/recipe-template.html for future recipes
- styles.css with page-specific styling centralized

Replace files in GitHub using the same folder structure.
Top-level pages go in the repo root.
Recipe files go in /recipes/.
The stylesheet goes in the repo root as styles.css.

The HTML files no longer contain <style> blocks.
